/**
 * This constant must not be modified because may produce problems :).
 */
export const GRAPHQL_SERVER_URI: string = "http://home.arpa:8000/api/v1/";

export const CLIENT_NAME: string = "agrovar-web-app";

export const CLIENT_VERSION: string = "0.1.0";

/**
 * This headers must not be modified because may produce problems :).
 */
export const GRAPHQL_SERVER_HEADERS: Record<string, string> = {
  Authorization: "none",
};
